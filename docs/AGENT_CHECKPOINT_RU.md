# AGENT CHECKPOINT

## Что это за проект

`GOSHA_PLATFORM` — отдельный собственный подготовительный контур для `Гоша`, который больше не должен зависеть от `xiaozhi.me` как от обязательной платформы привязки.

## Как писать новые записи

- Все новые планы, отчёты, объяснения и контрольные точки оформляй русским техническим языком по правилам из `../AGENTS.md`.
- Не используй необъяснённый английский жаргон в обычном тексте.
- Английский оставляй только для команд, путей, имён файлов, веток, коммитов и кода.

## Текущая точка

- Локальная базовая связка панели и шлюза уже существует.
- Добавлен отдельный внутренний шлюз ИИ-агентов `gosha-agent-gateway`.
- Добавлен слой хранения профилей агентов и привязки их к роботам поверх файлового слоя хранения.
- Панель получила операторские маршруты для:
  - списка профилей ИИ-агентов;
  - сохранения профиля;
  - чтения активной привязки профиля к роботу;
  - смены активного и резервного профиля робота.
- Папка уже превращена в самостоятельный git-репозиторий.
- Ветка публикации сейчас:
  - `agent/bootstrap-gosha`
- Первая отправка изменений в GitHub уже сделана.
- Короткая каноническая точка входа для следующего агента теперь вынесена в:
  - `docs/NEW_CHAT_CHECKPOINT_RU.md`
- Внешний пользовательский язык проекта переведён на бренд `Гоша`.
- Упоминания `Xiaozhi` оставлены только как внутренний технический слой совместимости, а не как название продукта.
- Для OTA и `activate` уже введены собственные продуктовые маршруты:
  - `/gosha/ota/`
  - `/gosha/ota/activate`
  Старые `/xiaozhi/ota/` и `/xiaozhi/ota/activate` оставлены как совместимые псевдонимы.
- Для сервера выбран отдельный корень `/opt/gosha_platform`.
- Для подготовительного поднятия выбраны:
  - панель и API: `151.241.228.232:18876`
  - узел `WebSocket`: `151.241.228.232:18080`
- Рабочая копия на сервере в `/opt/gosha_platform/app` выровнена с текущим состоянием ветки `agent/bootstrap-gosha`; на момент последней живой проверки она находилась на коммите `579c1ae`.
- Рабочая копия на сервере в `/opt/gosha_platform/app` сейчас уже доведена до коммита `a697a50`.
- Серверное развёртывание теперь поддерживает фазы:
  - `--phase panel`
  - `--phase backend`
  - `--phase all`
- На сервере уже подтверждён лёгкий подготовительный контур:
  - `gosha-agent-gateway.service` -> `active`
  - `gosha-panel.service` -> `active`
  - `gosha-observer.timer` -> `active`
  - `gosha-backend.service` -> `failed`, но считается допустимым отдельным следующим этапом текущей схемы
- В панели уже выведен удобный операторский путь привязки:
  - новый робот появляется в верхнем мигающем блоке
  - там же выбирается `robot_id`
  - там же нажимается `Привязать сейчас`
  - нижний блок `Платформа Гоша` остаётся как полный журнал и резервный путь
- Вход в панель уже восстановлен:
  - логин оператора работает
  - маршрут `/api/operator/login` подтверждён ответом `200`
  - пароль панели хранится только в `/opt/gosha_platform/runtime/env/panel.password`
- Наблюдатель подтверждает итог `OK`; отсутствие `backend` и порта `18080` помечается как необязательное предупреждение, а не как общий отказ контура.
- На сервере подтверждено, что один робот может:
  - появиться из неизвестного устройства через `pending`
  - быть привязан к существующей карточке
  - получить рабочий `activate`
  - наследовать профиль по умолчанию
  - затем получить явную привязку на другой профиль
  - сменить профиль без перепрошивки
- Текущее тестовое устройство уже привязано к `gosha-main`, но ещё молчит, потому что серверный `backend` не поднят.
- Локально подтверждено, что один робот может:
  - наследовать профиль по умолчанию;
  - затем получить явную привязку на другой профиль;
  - сменить профиль без перепрошивки.
- Создан отдельный локальный репозиторий `/home/max/GOSHA_FIRMWARE` как следующий независимый контур для собственной прошивки `Гоша`.
- Тестовая прошивка уже переведена на новый внешний OTA-путь:
  - `http://151.241.228.232:18876/gosha/ota/`
- Голосовой путь `WebSocket` пока сознательно не переименован и остаётся:
  - `ws://151.241.228.232:18080/xiaozhi/v1/`
  Это техническое ограничение совместимости текущего backend, а не продуктовая цель.

## Главные ограничения

- Не трогать `/opt/ai_robot` деструктивно.
- Не публиковать `local_only/`.
- Не возвращать жёсткие зависимости на `xiaozhi.me`.
- Не смешивать `GOSHA_PLATFORM` и `GOSHA_FIRMWARE` в одном рабочем дереве.
- Не тянуть исходники прошивки в `GOSHA_PLATFORM`.

## Ближайший приоритет

- При нормальном канале вернуться к серверу только для отдельной тяжёлой фазы:
  - `cd /opt/gosha_platform/app`
  - `bash ops/install_server.sh --phase backend`
- До запуска тяжёлой фазы можно уже безопасно привязывать новые устройства через верхний блок панели и подтверждать переход `pending -> claimed`.
- После запуска тяжёлой фазы первым делом проверить, что порт `18080` начал слушаться и что `gosha-main` перестал молчать.
- После этого отдельно решить судьбу голосового маршрута:
  - либо временно оставить `/xiaozhi/v1/` как внутренний совместимый путь
  - либо добавить безопасный псевдоним `/gosha/v1/` поверх уже работающего backend
- После подтверждения `backend` перейти в `/home/max/GOSHA_FIRMWARE`:
  - подтвердить русский пользовательский слой на устройстве
  - подтвердить новый внешний OTA-путь `/gosha/ota/`
  - окончательно заполнить аппаратный манифест и pin map
  - затем переходить к следующему этапу серверно-прошивочной совместимости
