# AGENT CHECKPOINT

## Что это за проект

`GOSHA_PLATFORM` — отдельная масштабируемая платформа `Гоша` для роботов, панели оператора и совместимого голосового серверного узла.

## Как писать новые записи

- Пиши отчёты, планы и контрольные точки русским техническим языком по правилам из `../AGENTS.md`.
- Не используй необъяснённый англо-русский суржик в обычном тексте.

## Текущая рабочая точка

- Ветка:
  - `agent/bootstrap-gosha`
- Актуальный верхний коммит смотри через:
  - `git -C /home/max/GOSHA_PLATFORM log --oneline -1`
- На сервере рабочая копия `/opt/gosha_platform/app` выровнена с этой веткой.

## Что уже работает

- Панель на `151.241.228.232:18876`
- Внутренний шлюз ИИ-агентов на `127.0.0.1:18110`
- Совместимый голосовой `WebSocket` на `151.241.228.232:18080/xiaozhi/v1/`
- Привязка устройства `pending -> claim -> activate`
- Новый верхний сценарий привязки робота в панели
- Новая панель управления ассистентом:
  - поставщики ИИ
  - ассистенты
  - голоса
  - память
  - `MCP`
  - экран и лица
  - пробуждение
  - база знаний
  - привязка к роботу
- Новые маршруты панели:
  - `GET/POST /api/operator/assistant-profiles`
  - `GET/POST /api/operator/voice-profiles`
  - `GET/POST /api/operator/memory-profiles`
  - `GET/POST /api/operator/mcp-bundles`
  - `GET/POST /api/operator/knowledge-profiles`
  - `GET/POST /api/operator/screen-profiles`
  - `GET/POST /api/operator/wake-profiles`
  - `GET/POST /api/operator/robots/<robot_id>/assistant-config`
  - `GET /api/operator/assistant-control/catalog`
- Внутренний `OpenAI`-совместимый прокси панели:
  - `GET /api/internal/openai/v1/models`
  - `POST /api/internal/openai/v1/chat/completions`
- На сервере уже есть базовый набор профилей и привязка `gosha-main` к профилю `assistant-gosha-default`.
- На сервере уже подключён рабочий ключ `DeepSeek` в:
  - `/opt/gosha_platform/runtime/env/providers.env`
- Внутренний прокси уже подтвердил живой ответ модели через `DeepSeek`.
- Совместимый голосовой `backend` уже исправлен для русского контура:
  - модель `deepseek-v4-flash`
  - распознавание речи переведено на `VoskASR`
  - на сервер загружена русская модель `vosk-model-small-ru-0.22`
- Реальное устройство `dc:b4:d9:35:1b:e0` уже привязано к `gosha-main`.
- После привязки `OTA` уже отдаёт устройству рабочий блок:
  - `ws://151.241.228.232:18080/xiaozhi/v1/`
- На реальном устройстве уже подтверждено:
  - срабатывание слова пробуждения `Гоша`
  - открытие голосового `WebSocket`
  - распознавание русской фразы `Привет`
  - голосовой ответ робота на русском
- Для панели и живого голосового контура уже добавлены новые русские профили голоса:
  - `voice-ru-default` — Светлана, основной женский голос
  - `voice-ru-man` — Дмитрий, взрослый мужской голос
  - `voice-ru-kid-girl` — детский пресет на базе Светланы
  - `voice-ru-kid-boy` — детский пресет на базе Дмитрия
- На сервере уже работает расширенный модуль `EdgeTTS`, который умеет применять:
  - имя голоса
  - скорость речи
  - высоту голоса
- Системный промпт `assistant-gosha-default` усилен:
  - ассистент должен отвечать только по-русски
  - ассистент не должен самопроизвольно переходить на китайский, японский или английский
  - при плохом распознавании он должен просить повторить фразу по-русски
- На сервере дополнительно подтверждено:
  - `VoskASR` распознаёт русскую тестовую фразу `привет гоша как дела`
  - внутренний прокси панели больше не передаёт повторяющиеся описания инструментов в поставщика ИИ
- Пакет `mobile API` уже расширен новым разделом `bundle.mobile_profile`.
- Для платформы создан отдельный Android-проект:
  - `/home/max/GOSHA_MOBILE`
- Новый мобильный проект уже собирается локально как самостоятельный клиент:
  - `assembleClientDebug`
  - `testClientDebugUnitTest`
- Выполнен живой прогон нового клиента на телефоне `TECNO LI9`:
  - код `MJ6SG97A` принят
  - приложение сохранило `panel_url = http://151.241.228.232:18876`
  - приложение сохранило `hub_url = ws://151.241.228.232:18080/mcp/`
  - приложение сохранило `robot_id = gosha-main`
  - приложение перевело телефон в сеть `GOSHA-A-1BE1`
  - локальный экран `HotspotPortalActivity` открыт

## Главный незакрытый блокер

- Базовый живой голос уже работает.
- Текущий технический хвост теперь не в связности, а в качестве и устойчивости:
  - нужно повторно проверить серию повторных пробуждений после перехода на `VoskASR`
  - нужно повторно проверить серию повторных русских запросов после перехода на `VoskASR`
  - нужно проверить, ушёл ли самопроизвольный китайский язык после усиления системного промпта
  - нужно вживую оценить новый мужской голос и оба детских пресета
  - нужно оценить влияние несовпадения частоты:
    - сервер `16000`
    - устройство `24000`
- Отдельный оставшийся хвост живого устройства:
  - иногда есть ложное срабатывание без слова `Гоша`
  - распознавание заметно лучше на близкой дистанции, чем издали
  - это уже похоже на порог пробуждения, акустику корпуса, микрофонный тракт или влияние динамика
- Для мобильного контура ближайший незакрытый шаг теперь пользовательский:
  - робот может подключиться через приложение
  - но локальный портал `192.168.4.1` в одном из прогонов давал пустую страницу
  - значит мобильный сценарий надо дополировать отдельно

## Ближайший приоритет

1. На устройстве `gosha-main` прогнать несколько подряд живых голосовых циклов.
2. Через панель проверить переключение между:
   - `voice-ru-default`
   - `voice-ru-man`
   - `voice-ru-kid-girl`
   - `voice-ru-kid-boy`
3. Проверить, не уходит ли ассистент в китайский после усиления русского системного промпта.
4. Проверить качество звука и решить вопрос частоты `16000 -> 24000`.
5. Отдельным следующим пакетом в `GOSHA_FIRMWARE` заняться:
   - порогом слова пробуждения
   - ложными срабатываниями
   - дальним распознаванием
   - влиянием динамика на самопробуждение
6. Если потребуется смена ключа или профиля поставщика, перезапустить лёгкий контур:
   - `bash /opt/gosha_platform/app/ops/install_server.sh --phase panel`
7. Проверить:
   - `POST /api/internal/openai/v1/chat/completions`
   - что `gosha-main` устойчиво отвечает голосом
   - что в панели нет критичной ошибки по поставщику ИИ
8. Проверить новый мобильный контур:
   - подтверждённые шаги:
     - установка `GOSHA_MOBILE`
     - ввод кода подключения
     - поиск `GOSHA-*`
     - итоговое подключение робота к домашнему `Wi‑Fi`
   - незакрытые шаги:
     - живой радиотест переходного `Xiaozhi-*`
     - устойчивое рабочее открытие `192.168.4.1`
     - переход в главное меню после настройки робота без лишних перезапусков
9. Потом переходить к второй очереди:
   - применение экранных профилей
   - применение пробуждения
   - рабочая база знаний
