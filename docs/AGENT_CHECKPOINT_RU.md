# AGENT CHECKPOINT

## Свежая контрольная точка 2026-09-05

- Для задачи `task-20260905-gosha-platform-ci-candidate` подготовлен scoped кандидат поверх PR66 `4375a6d0415c07f9b09c3a1b4e0135857ed5d9e1`.
- GitHub workflow `repo-validation` теперь запускается для stacked Draft PR: у `pull_request` убрано ограничение base-веткой `main`, добавлены события `edited` и `converted_to_draft`.
- Workflow остаётся read-only: `permissions: contents: read`, обычный `pull_request`, `actions/checkout` с `persist-credentials: false`, без обращения к GitHub secrets.
- `bin/ci_validate.sh` сохранён как единая CI-команда и дополнен статической проверкой безопасности workflow.
- Добавлен `docs/GOSHA_STACKED_PR_ACCEPTANCE_RU.md` с воспроизводимым smoke/read-only acceptance списком для panel/Android/firmware, включая network recovery, identity, runtime events, no-motion и `20 warm + 5 cold` voice-turn measurement.
- Из PR58 перенесены только безопасные документационные замены реальных owner-only путей и адресов на маркеры вроде `<LOCAL_WORKSPACE>`, `<PRIMARY_PLATFORM_SERVER>`, `<SERVER_PROVIDER_ENV>`.
- Не откатывать PR66 voice-turn документацию: контракт `voice.turn.phase`, агрегация `voice_turns`, запрет transcript/prompt/endpoint/secret/raw identity и расчёт только до `robot_first_audio_out` должны остаться текущими.
- Не выполнялись commit, push, rebase, live deploy, production access, Android/firmware edits, relay/controller/hardware/iOS/pins edits, motion и operator-command gateway.

## Свежая контрольная точка 2026-09-04

- Для задачи `task-20260904-gosha-platform-voice-turn-latency-contract` реализован только платформенный контракт задержки голосового оборота.
- `gosha.runtime.event.v1` расширен типом `voice.turn.phase`. Для него обязательны `trace.correlation_id`, `task.id`, `voice.phase` и безопасный `voice.warm_state`.
- Агрегация в runtime-снимке идёт в блок `voice_turns` по паре `trace.correlation_id` и `task.id`.
- Воспринимаемая задержка считается только по фазам `user_speech_end -> robot_first_audio_out`, причём `robot_first_audio_out` принимается только от источника `robot`. Серверная `tts_first_audio` без прошивочного подтверждения не закрывает измерение.
- Поддержаны состояния прогрева `warm`, `cold`, `unknown`; пороги: `warm 1800/2500 ms`, `cold 4500/6000 ms`. Для `unknown` измерение сохраняется без оценки порога.
- Недоступный первый ответ LLM фиксируется фазой `llm_first_token_unavailable` и не ломает расчёт воспринимаемой задержки.
- Вход `voice.turn.phase` и сохранённый вывод защищены от transcript, prompt, URL, token, SSID, raw `device_id`, MAC, IP и raw audio.
- Прошивочные события через `/gosha/events` и `/xiaozhi/events` теперь получают `source.id = robot-claim-*` из доверенной привязки, без раскрытия аппаратного `device_id`.
- Живые сервисы, робот, serial, развёртывание, секреты, Android и прошивка не трогались.

## Свежая контрольная точка 2026-09-04

- В задаче `task-20260904-gosha-platform-voice-samplerate-latency` серверный голосовой путь разобран как `ASR -> LLM -> TTS`.
- Причина предупреждения `16000/24000` находится в платформенном рендере совместимого backend: значение по умолчанию внешнего backend `xiaozhi.audio_params.sample_rate = 24000` расходилось с фактическим контрактом `gosha-v1`, входным Opus-декодером backend и `VoskASR`, которые работают на `16000`.
- Минимальное исправление подготовлено в git-слое платформы:
  - `ops/render_backend_config.py` явно пишет `xiaozhi.audio_params.format = opus`, `sample_rate = 16000`, `channels = 1`, `frame_duration = 60`;
  - частота берётся из настройки `SELFHOST_XIAOZHI_AUDIO_SAMPLE_RATE`, значение по умолчанию `16000`;
  - `ops/install_server.sh` добавляет эту настройку в `selfhost-backend.env` и передаёт её в рендер;
  - `backend/selfhost-backend.env.example` показывает отдельный ключ `SELFHOST_XIAOZHI_AUDIO_SAMPLE_RATE=16000`.
- Важно не смешивать две частоты:
  - `SELFHOST_XIAOZHI_AUDIO_SAMPLE_RATE=16000` — частота Opus-контракта устройства и приветственного `xiaozhi.audio_params`;
  - `SELFHOST_XIAOZHI_SILERO_SAMPLE_RATE=24000` — внутренняя частота генерации `SileroTTS v5_5_ru`, потому что официальный `Silero` для `v5_5_ru` поддерживает `8000/24000/48000`, но не `16000`.
- Задержка первого ответа после прогрева сейчас в основном формируется интервалом `ASR -> первый ответ LLM -> первый TTS-синтез`. Холодный оборот диалога дополнительно платит за загрузку и прогрев `SileroTTS`. Текущая правка выравнивает сетевой контракт и убирает предупреждение о частоте, но для отдельного сокращения холодного старта нужен следующий измерительный срез без обращения к живому роботу из этой задачи.
- Добавлен тест `ops/test_render_backend_audio_contract.py`; `bin/ci_validate.sh` теперь запускает его отдельным шагом.
- Не выполнялись: развёртывание, правка `<SERVER_PROVIDER_ENV>`, обращения к роботу, проверка raw `8080`, изменения Android, прошивки или AI Office.

## Свежая контрольная точка 2026-08-26

- Работу продолжать с Draft PR платформы `#46`; Android PR `#51` и firmware gate не начинать до его терминального `PASS`.
- AI Office исправлен: фактический распределённый reviewer запускается на точном профиле `GPT-5.5 / xhigh`. Временный worker после задачи снова переведён в `draining`.
- Последний reviewer платформы дал `NO-GO` с двумя `P2`, которые уже исправлены локально:
  - SQLite journal/snapshot/list/duplicate paths проверяют secret-safe форму сохранённого события и совпадение `subject.robot_id` с областью строки;
  - автоматический self-host fallback разделяет `PUBLIC_PANEL_URL` на HTTP `18876` и voice/MCP `18080`; локальный launcher использует те же роли.
- Первый re-review подтвердил эти пути, но нашёл ещё один `P2` в доверии к агрегированному DB/legacy snapshot с пересчитанным digest. Весь snapshot теперь проходит рекурсивную secret-safe проверку до принятия.
- Следующий точный re-review нашёл дополнительный `robot_id` вне канонического `subject`. Теперь identity-ключ разрешён только в корне snapshot и каноническом `subject`; в event/component/task дополнительных полях он fail closed.
- Последующий re-review нашёл stale DB snapshot с корректным digest, но старым journal tip. `_db_snapshot_row()` теперь сверяет `last_event_rowid/last_event_id` с реальным концом SQLite-журнала и не принимает lifetime total меньше retained count.
- Новые adversarial и migration-тесты для journal, snapshot и identity прошли, повторный полный `bash bin/ci_validate.sh` завершил 10/10 этапов.
- Следующий ход: commit/push платформенной ветки, дождаться GitHub checks и повторить исходную reviewer-задачу в AI Office. Только после `PASS` переходить к Android.
- Не выполнять живое развёртывание, flash, motion, trim или servo sequence: левая серва неисправна. `TEMP_NL_RELAY` остаётся временной символической ролью до `FUTURE_PRODUCTION_SERVER`.

## Свежая контрольная точка 2026-08-25

- Новый физический робот живьём подтверждён в треугольнике `робот — Android — панель`. Текущий рабочий сетевой обход — роль `TEMP_NL_RELAY`, ведущая на `PRIMARY_PLATFORM_SERVER`.
- Логика ролей:
  - `PRIMARY_PLATFORM_SERVER` остаётся источником истины для панели, `mobile API`, OTA, голосового контура, профилей и runtime-состояния;
  - `TEMP_NL_RELAY` только прокидывает нужные публичные порты и не содержит full stack `Гоши`;
  - `FUTURE_PRODUCTION_SERVER` — будущий нормальный серверный адрес, на который нужно переехать в конце месяца.
- Публичные роли портов:
  - `18876` — HTTP-панель, `mobile API`, OTA и привязка устройства;
  - `18080` — голосовой `WebSocket` и совместимый голосовой `MCP`-канал.
- В git не добавлять реальные IP-адреса, секреты, коды подключения, Wi-Fi-пароли, файлы подписи и локальное runtime-состояние. Не хардкодить `TEMP_NL_RELAY` в коде или документации как постоянный product default.
- Старые `8876/8890` нельзя возвращать как product defaults. Если нужен совместимый слой, он должен быть явно описан как legacy/reference, а не как текущая платформа.
- Живая приёмка:
  - новый робот привязан к серверной личности `gosha-main`;
  - Android показывает `Гоша Main` и подтверждает робота в сети;
  - голосовой цикл с пробуждением `Гоша`, русским распознаванием и русским ответом подтверждён;
  - движения, `set_trim`, servo sequence и ручные подталкивания под питанием запрещены до замены левой сервы или кабеля.
- Платформенный quality-gate для `feature/triangle-runtime-events @ 757feffcde587c685aac83eee095b0d1853769ea` дал `PASS` на открытие отдельного чернового Pull Request:
  - рабочее дерево чистое;
  - ветка синхронизирована с `origin/feature/triangle-runtime-events`;
  - `origin/main` — `e61ad84f39464800860f958ef660a2dddf953a1e`;
  - отличие от `main`: `ahead 9 / behind 0`, 21 файл;
  - прошли `PYTHONDONTWRITEBYTECODE=1 bash bin/ci_validate.sh`, `platform/test_gosha_runtime_events.py`, `platform/test_runtime_event_http_contract.py`, `platform/test_selfhost_runtime_events.py` и `git diff --check origin/main...HEAD`.
- Pull Request для платформы ещё нужно открыть; GitHub checks и независимый review ещё не пройдены.
- Общий integration quality-gate остаётся открытым до отдельных Draft PR и проверок в порядке `платформа → Android → прошивка`, а затем короткой живой приёмки после переезда с временного relay на стабильный серверный адрес.
- `feature/operator-command-gateway` и любые операторские команды не начинать до закрытия этого quality-gate.

## Предыдущая контрольная точка 2026-08-24

- Новый робот принял параметры домашнего Wi-Fi, но получает системную ошибку отсутствия маршрута до публичного узла платформы. Это внешний сетевой блокер, а не ошибка локальной службы или пароля Wi-Fi.
- Существующая привязка `gosha-main` принадлежит старому физическому роботу. Не удалять и не переиспользовать её без явного решения пользователя.
- В `gui_panel.py` добавлен временной барьер: мобильный onboarding получает `device_id` только из привязки, созданной строго после текущего кода подключения.
- Новый тест покрывает старую привязку, свежую привязку и новый код после неё; полный платформенный CI из семи шагов прошёл.
- Изменение не развёрнуто. Не заявлять живую регистрацию нового робота до восстановления прямой доступности публичного узла и новой приёмки.

## Предыдущая контрольная точка 2026-07-30

- Текущая стадия: интеграция уже проверенного общего контура, а не продолжение разработки в старой ветке.
- Рабочее дерево платформы: `<PLATFORM_TRIANGLE_WORKTREE>`.
- Ветка: `feature/triangle-runtime-events`; последний отправленный коммит перед этой документной фиксацией — `f2276f7`.
- Соседние проверенные вершины:
  - Android: `feature/mobile-triangle-runtime`, `d0ab7c2`;
  - прошивка: `feature/firmware-orange-eyes`, `8031010`.
- Все три рабочие копии чистые и совпадают со своими удалёнными ветками.
- Все три ветки остаются вне `main`; Pull Request для них на `2026-07-30` не создан.
- Основной каталог `<LOCAL_WORKSPACE>` находится на старой ветке `codex/reviewer-executor-polish-20260519`, опережает её удалённую ветку на два коммита и не является рабочей копией текущего продуктового направления. Не переносить из него изменения в профильную ветку автоматически.
- ИИ-офис локально не запущен; последний сохранённый снимок имеет дату `2026-07-23`, активных задач `gosha-platform` в нём нет.
- Следующий агент должен:
  1. запустить и проверить ИИ-офис;
  2. повторить проверки трёх точных веток;
  3. подготовить отдельные черновые Pull Request;
  4. не начинать шлюз операторских команд до решения интеграционного этапа.
- После принятия трёх веток создать отдельное рабочее дерево `platform-operator-command-gateway` от обновлённого `origin/main` и начать с контракта `gosha.robot.command.v1`, имитатора робота и команды `status`.

## Предыдущая контрольная точка 2026-07-23

- Текущее отдельное рабочее дерево: `<PLATFORM_TRIANGLE_WORKTREE>`.
- Ветка: `feature/triangle-runtime-events`, база `origin/main` (`e61ad84`).
- Направление: масштабируемый общий контур «робот — приложение — панель».
- Контракт: `docs/GOSHA_RUNTIME_TRIANGLE_CONTRACT_RU.md`.
- Платформа уже подготовлена принимать проверенные события от нескольких прошивок, мобильных установок и операторских панелей, агрегировать их по `robot_id` и обновлять операторский интерфейс через поток событий.
- Первый сквозной контрольный сценарий — восстановление Android после временной потери домашнего Wi-Fi без перезапуска приложения.
- Повторная независимая проверка ИИ-офиса `task-20260722T093653Z-read-only-runtime-triangle` завершена без P0/P1/P2; сборки и локальные проверки пройдены.
- Работа разделена между соседними репозиториями:
  - Android: `<MOBILE_TRIANGLE_WORKTREE>`, ветка `feature/mobile-triangle-runtime`;
  - прошивка: `<FIRMWARE_TRIANGLE_WORKTREE>`, ветка `feature/firmware-orange-eyes`.
- Кодовый снимок платформы `87b24bb` уже развёрнут на живом сервере; последняя отправленная контрольная фиксация перед этим планом — `747d663`. Итоговый Android-коммит — `d0ab7c2`.
- Первый прошивочный прогон выявил гонку времени жизни `HttpClient`/`EspTcp` и был немедленно отменён возвратом стабильного образа. Исправление TCP/TLS прошло финальную проверку ИИ-офиса без P0/P1/P2 и находится в `af4de9c`; тёмно-оранжевая тема — в `a063569`, последняя отправленная контрольная фиксация прошивки — `8031010`.
- Исправленная прошивка установлена только в раздел приложения и проработала контрольное окно более 210 секунд без паники или перезапуска. Живое хранилище принимает `robot.runtime.heartbeat`, а `WSControl` продолжает проверочные обмены.
- `2026-07-23` полный Android P1 повторён на точном текущем APK и установленной тёмно-оранжевой прошивке:
  - PID приложения сохранился;
  - возврат сети не закрыл задачу преждевременно;
  - `completed` появился только после локального подтверждения робота;
  - очередь Android пуста;
  - робот продолжал присылать свежие heartbeat-события;
  - поток панели в реальном времени и OTA-контракт подтверждены.
- Безопасная команда панели `self.otto.get_status` остаётся неподтверждённой: режим `cloud-mcp` обрывается после `initialize_sent`. Это следующий P1 миграции управления на собственный шлюз, а не основание считать физический робот подтверждённо управляемым из панели.
- Канонический план этого P1 сохранён в `docs/GOSHA_OPERATOR_COMMAND_GATEWAY_PLAN_RU.md`.
- План зафиксирован и отправлен коммитом `30d1c2d`. Финальный reviewer ИИ-офиса `task-20260723T124225Z-read-only-review-30d1c2d-home-max-worktrees` подтвердил отслеживание файла, наличие коммита в удалённой ветке и отсутствие P0/P1/P2.
- В плане `stop` вынесен в отдельный автомат `safety_stopped`: он не ждёт за обычной очередью, не выполняется после истечения срока и не разрешает новые побочные действия до отдельного авторизованного снятия блокировки.
- Следующий агент не должен сразу править старый облачный мост. Сначала нужно подготовить отдельную ветку и рабочее дерево, зафиксировать `gosha.robot.command.v1` и поднять сквозной `status` через имитатор робота; только затем подключать живую прошивку.
- Обезличенное доказательство: `<OWNER_ONLY_ACCEPTANCE_EVIDENCE>`, SHA-256 `600c3d526dddf95a39c38c1cf126a952d02a2e6b7b506f8a591b6f94d7690f0e`.
- В `main` ничего не слито. Вручную остаются визуальная проверка физического экрана и один голосовой диалог после финальной пары приложения и ресурсов.

## Что это за проект

`GOSHA_PLATFORM` — отдельная масштабируемая платформа `Гоша` для роботов, панели оператора и совместимого голосового серверного узла.

## Как писать новые записи

- Пиши отчёты, планы и контрольные точки русским техническим языком по правилам из `../AGENTS.md`.
- Не используй необъяснённый англо-русский суржик в обычном тексте.

## Историческая базовая точка до профильных рабочих деревьев

Этот блок сохранён как история раннего развёртывания. Он не заменяет свежую контрольную точку выше и не задаёт текущую ветку следующей работы.

- Ветка:
  - `agent/bootstrap-gosha`
- Актуальный верхний коммит смотри через:
  - `git -C <LOCAL_WORKSPACE> log --oneline -1`
- На сервере рабочая копия `<SERVER_APP_ROOT>` выровнена с этой веткой.
- Главная карта связанных контуров:
  - `<LOCAL_WORKSPACE>/docs/GOSHA_PROJECT_MAP_RU.md`

## Что уже работает

- Панель на `<PRIMARY_PLATFORM_SERVER>:18876`
- Внутренний шлюз ИИ-агентов на `127.0.0.1:18110`
- Совместимый голосовой `WebSocket` на `<PRIMARY_PLATFORM_SERVER>:18080/xiaozhi/v1/`
- Привязка устройства `pending -> claim -> activate`
- Новый верхний сценарий привязки робота в панели
- Главный операторский экран панели уже упрощён:
  - на первом экране оставлены только:
    - пульт оператора
    - карта Wi‑Fi роботов
    - состояние флота
    - мобильные приложения
    - верхняя статистика
    - подключение нового робота
  - отдельная рабочая страница выбранного робота
  - у страницы робота есть собственная прямая ссылка по URL
  - полный каталог роботов убран с первого экрана и спрятан в дополнительное действие
  - детальный раздел `Личность, модель и голос` теперь снова прямо внутри рабочего окна
  - вкладка `Клиент, память и инструменты` теперь тоже рабочая и заметная для оператора:
    - память видна и редактируется как отдельный профиль;
    - набор инструментов и MCP виден и редактируется прямо на странице робота;
    - код подключения остался в этой вкладке, но перестал быть её единственной функцией
  - в рабочем окне можно вручную менять:
    - системный промпт
    - модель
    - поставщика ИИ
    - голосовой движок
    - голос
    - темп и высоту голоса
  - в панели появился обучающий контур:
    - в шапке есть кнопка `Справка`
    - на первом экране есть короткая памятка по работе с пультом
    - рядом с ключевыми полями стоят `?`-подсказки
    - особенно подробно объясняются режимы подключения, резервные профили, память, инструменты и выбор TTS
- Для роботов на `Платформе Гоша` панель теперь показывает прямой след новой прошивки:
  - когда робот сам вышел на связь;
  - какую плату он сообщил;
  - какую версию прошивки он сообщил;
  - какой адрес увидел сервер
  - этот сигнал вынесен в отдельный блок `След устройства в платформе`
- В открытой панели больше не показываются прямые OTA-URL и детали нашей прошивки.
  OTA остаётся внутренним сервисным контуром платформы.
- Локальный режим панели поднимается только вручную:
  - `bash <LOCAL_WORKSPACE>/bin/run_local_gosha_gateway.sh`
  - `bash <LOCAL_WORKSPACE>/bin/run_local_gosha_panel.sh`
  - только после этого доступен:
    - `http://127.0.0.1:18876`
- Panel runtime теперь сам проверяет Python-модуль `websockets`:
  - `bin/ensure_panel_python_deps.sh`
  - без него честная live-проверка робота не работает
- Если разработчик запускает команды из `<HOME_WORKSPACE_ROOT>`, на этой машине уже есть совместимые wrapper-скрипты:
  - `bash <LOCAL_BIN>/run_local_gosha_gateway.sh`
  - `bash <LOCAL_BIN>/run_local_gosha_panel.sh`
- Для честной локальной проверки всей цепочки теперь есть отдельная команда:
  - `bash <LOCAL_WORKSPACE>/bin/check_local_gosha_stack.sh`
  - или alias:
    - `bash <LOCAL_WORKSPACE>/bin/check_gosha_panel_stack.sh`
  - она подтверждает не только открытый порт, а живые ответы panel → gateway и выводит след первого робота в платформе
  - теперь дополнительно показывает:
    - `service_state`
    - `detection_state`
    - `protocol_phase`
    - `error_type`
    - `diagnosis_hint`
  - это помогает сразу понять, проблема в остановленном сервисе, разрыве после `initialize`, timeout на `tools/call` или в отсутствии прямого сигнала прошивки
- `init_local_lab.sh` теперь ещё и мигрирует legacy локальные карточки в self-hosted режим:
  - `gosha-main` больше не должен оставаться локально в `xiaozhi_cloud`
  - после миграции локальный `create-code` для `gosha-main` отдаёт `edge_hub_url = ""` по умолчанию; поле заполняется только явно настроенным `PUBLIC_EDGE_HUB_URL`
- До отдельного интеграционного quality-gate этот слой остаётся presence-only:
  - не подставлять voice WebSocket `/mcp` как `edge_hub_url`
  - не начинать operator-command gateway из этой ветки
- В карточке и на странице робота теперь есть отдельный операторский блок:
  - `Где искать проблему`
  - он показывает не просто цвет, а зону возможного сбоя:
    - панель / MCP-адрес
    - ключ доступа
    - `gateway / bridge`
    - домашний Wi-Fi / hotspot
    - `robot -> panel`
    - `panel -> gateway -> robot`
  - и сразу даёт оператору следующий безопасный шаг
  - дополнительно уже различает живые сценарии:
    - сессия оборвалась сразу после `initialize`
    - handshake дошёл до `tools/call`, но робот не прислал `ACK`
    - карточка уже в `Платформе Гоша`, но робот всё ещё смотрит на внешний `api.xiaozhi.me`
- Операторская диагностика `edge-hub` теперь честно различает свежую проверку и кеш:
  - `status.robot_ws_probe_state = executed` оставляет `reported-ready` / `reported-unreachable`;
  - `status.robot_ws_probe_state = skipped` показывает `cached-ready` / `cached-unreachable` и возраст кеша из `status.robot_ws_probe_cached_age_ms`;
  - `status.robot_ws_probe_state = stale` не превращается в `reported-ready`, даже если кеш раньше был успешным;
  - legacy-агенты без `robot_ws_probe_state` продолжают работать через `status.robot_ws_ok`;
  - точечная проверка лежит в `platform/test_edge_hub_transport_diagnostics.py`;
  - независимый review через AI_OFFICE нашёл и закрыл P2: неизвестный `robot_ws_probe_state` больше не может попасть в операторский HTML как разметка;
  - сервер нормализует состояние по белому списку `executed / skipped / stale / unknown`, а `diagPill` и подробности дополнительно используют `escapeHtml`;
  - регрессионный тест передаёт HTML-фрагмент как состояние и проверяет нормализацию вместе с legacy `robot_ws_ok`;
  - задача `task-20260712T092101Z-review-gosha-plat` прошла manager, reviewer, fixer и финальный reviewer без оставшихся P0/P1/P2;
  - каноническая ветка `hotfix/edge-hub-probe-state` содержит `10bcbf1` и защитный коммит `551cbb7`;
  - `git diff --check`, синтаксическая проверка Python без записи `pyc` и `python3 -B platform/test_edge_hub_transport_diagnostics.py` проходят.
- Верх страницы конкретного робота теперь ещё проще:
  - верхние поля `Связь`, `Сеть`, `Последний выход на связь`, `Плата и прошивка` и причина проблемы собраны в единый блок `Главный статус робота`
  - это заменило лишнее дублирование двух отдельных верхних панелей и сделало рабочую страницу робота понятнее для оператора
- Обзор страницы робота теперь разложен аккуратнее без слома вкладок:
  - после `Главного статуса робота` идёт короткий блок `Что делать с этим роботом сейчас`;
  - все основные кнопки собраны в отдельный блок `Кнопки управления`;
  - обзорные карточки теперь разделены по смыслу:
    - подключение
    - личность
    - модель и голос
    - клиент и коды
    - память и инструменты
    - след устройства в платформе
    - Wi‑Fi робота
  - это уже визуально проверено через локальные headless-снимки панели
- `gosha-main` теперь считается главным тестовым роботом и поднимается первым:
  - в карте Wi‑Fi роботов;
  - в состоянии флота;
  - в общем операторском списке роботов
- Текущий зафиксированный этап надо считать рабочей общей контрольной точкой:
  - `GOSHA_PLATFORM` уже чистый и синхронизирован с `origin/agent/bootstrap-gosha`;
  - `GOSHA_FIRMWARE` уже чистый и синхронизирован с `origin/main`;
  - `GOSHA_MOBILE` уже чистый и синхронизирован с `origin/main`;
  - на рабочем столе создан отдельный файл входа для нового чата по этому состоянию
- Вкладка подключения тоже стала понятнее без ломки backend:
  - кнопка `Открыть MCP` переименована в `Настроить подключение`
  - появился блок `Текущая схема подключения`
  - рядом с полями `Режим платформы`, `Режим подключения`, `Резервный профиль`, облачным MCP-адресом и резервным WebSocket-адресом есть понятные подсказки
- Главный экран стал короче для оператора:
  - над карточками теперь есть короткая сводка по флоту:
    - сколько реально на связи
    - сколько в hotspot
    - сколько недавно сами отметились прошивкой
    - сколько требуют внимания
  - быстрые действия разделены на:
    - основные
    - дополнительные
  - второстепенные кнопки скрываются под переключателем, чтобы стартовый экран не выглядел перегруженным
- Верхние KPI стартового экрана больше не подсказывают ложную “живость”:
  - счётчик `Wi‑Fi роботов` теперь опирается на подтверждённую живую связность
  - счётчик `Роботов в системе` показывает инвентарь боевых карточек, а не косвенную доступность
- Мобильный runtime-контур теперь тоже выровнен по честной server-side модели:
  - `/api/mobile/robots/<robot_id>/runtime` уже отдаёт `connectivity`
  - `connectivity` различает:
    - `local_host`
    - `probe_verified`
    - `fresh_device_contact`
    - отсутствие живых подтверждений
  - старая ложная эвристика Android "подключено через панель только по настроенному control" закрыта
- Новая панель управления ассистентом:
  - поставщики ИИ
  - ассистенты
  - движки синтеза речи
  - голоса
  - память
  - `MCP`
  - экран и лица
  - пробуждение
  - база знаний
  - привязка к роботу
- Новые маршруты панели:
  - `GET/POST /api/operator/assistant-profiles`
  - `GET/POST /api/operator/tts-engine-profiles`
  - `GET/POST /api/operator/voice-profiles`
  - `GET/POST /api/operator/memory-profiles`
  - `GET/POST /api/operator/mcp-bundles`
  - `GET/POST /api/operator/knowledge-profiles`
  - `GET/POST /api/operator/screen-profiles`
  - `GET/POST /api/operator/wake-profiles`
  - `GET/POST /api/operator/robots/<robot_id>/assistant-config`
  - `GET /api/operator/assistant-control/catalog`
- Внутренний `OpenAI`-совместимый прокси панели:
  - `GET /api/internal/openai/v1/models`
  - `POST /api/internal/openai/v1/chat/completions`
- На сервере уже есть базовый набор профилей и привязка `gosha-main` к профилю `assistant-gosha-default`.
- На сервере уже подключён рабочий ключ `DeepSeek` в:
  - `<SERVER_PROVIDER_ENV>`
- Внутренний прокси уже подтвердил живой ответ модели через `DeepSeek`.
- Совместимый голосовой `backend` уже исправлен для русского контура:
  - модель `deepseek-v4-flash`
  - распознавание речи переведено на `VoskASR`
  - на сервер загружена русская модель `vosk-model-small-ru-0.22`
- Реальное устройство `<DEVICE_ID>` уже привязано к `gosha-main`.
- После привязки `OTA` уже отдаёт устройству рабочий блок:
  - `ws://<PRIMARY_PLATFORM_SERVER>:18080/xiaozhi/v1/`
- На реальном устройстве уже подтверждено:
  - срабатывание слова пробуждения `Гоша`
  - открытие голосового `WebSocket`
  - распознавание русской фразы `Привет`
  - голосовой ответ робота на русском
- В слое ассистента теперь выделен отдельный профиль движка синтеза речи:
  - `tts-engine-edge-default` — текущий живой `EdgeTTS`
  - `tts-engine-silero-prep` — подготовка следующего русского `TTS`
  - голосовой профиль теперь ссылается на движок, а не считается самим движком
- Для панели и живого голосового контура уже добавлены новые русские профили голоса:
  - `voice-ru-default` — Светлана, основной женский голос
  - `voice-ru-man` — Дмитрий, взрослый мужской голос
  - `voice-ru-kid-girl` — детский пресет на базе Светланы
  - `voice-ru-kid-boy` — детский пресет на базе Дмитрия
- На сервере уже работает расширенный модуль `EdgeTTS`, который умеет применять:
  - имя голоса
  - скорость речи
  - высоту голоса
- Важно:
  - в панели и в API уже есть отдельный каталог `tts_engine_profile`
  - живой `backend` уже читает его при пересборке конфигурации
  - рендер живой конфигурации вынесен в `ops/render_backend_config.py`
  - если новый движок ещё не готов к живому применению, `backend` безопасно остаётся на `EdgeTTS`
  - это fallback-состояние теперь явно возвращается в `assistant_control` как `tts_runtime`
  - тот же рендерер теперь пишет в итоговый `.config.yaml` строку `effective-tts-runtime`
  - в репозитории уже появился первый собственный модуль `backend/overrides/silero_local.py`
  - `compose` уже монтирует его в совместимый серверный узел
  - рендерер уже умеет собрать рабочий блок `SileroTTS`, если профиль переведён в `ready`
  - на живом сервере уже поднят тестовый профиль `tts-engine-silero-live-test`
  - для него уже созданы тестовые голоса:
    - `voice-ru-silero-baya`
    - `voice-ru-silero-aidar`
    - `voice-ru-silero-kseniya`
  - найден и закрыт дефект молчания после слова `Гоша`:
    - рендерер ошибочно писал `type: local`
    - совместимый backend ожидал имя реального провайдера
    - теперь живой `.config.yaml` пишет `type: silero_local`
  - после этой правки `SileroTTS` уже подтверждён по живым логам на реальном роботе
  - затем найден и второй риск устойчивости:
    - старый `agent-base-prompt.txt` подталкивал backend к коротким китайским репликам
    - прямой тест `SileroTTS` показал, что на фразах `我在呢`, `有啥吩咐？`, `啥事这么开心呀`, `说来我也乐呵乐呵~` синтез падает
    - на русских коротких заменах `Я на связи`, `Что случилось?`, `Рассказывай, я слушаю` синтез проходит успешно
    - для этого уже добавлен и выкачен на сервер русский override:
      - `backend/overrides/agent-base-prompt.txt`
  - текущий `EdgeTTS` даёт два базовых русских тембра и два детских пресета на их основе
  - нативных отдельных русских детских голосов в текущем живом контуре нет
  - следующий архитектурный шаг по голосу — отдельный переход на новый движок синтеза речи
- Закрыт дефект применения голоса после сохранения из панели:
  - раньше новый голос сохранялся, но живой `backend` продолжал говорить старым голосом
  - теперь сохранение ассистента, голоса и привязки робота автоматически пересобирает только голосовой `backend`
  - живой тест уже подтверждён через сохранение `assistant-gosha-default`
- Системный промпт `assistant-gosha-default` усилен:
  - ассистент должен отвечать только по-русски
  - ассистент не должен самопроизвольно переходить на китайский, японский или английский
  - при плохом распознавании он должен просить повторить фразу по-русски
- На сервере дополнительно подтверждено:
  - `VoskASR` распознаёт русскую тестовую фразу `привет гоша как дела`
  - внутренний прокси панели больше не передаёт повторяющиеся описания инструментов в поставщика ИИ
- Пакет `mobile API` уже расширен новым разделом `bundle.mobile_profile`.
- Для платформы создан отдельный Android-проект:
  - `<MOBILE_WORKSPACE>`
- Для `GOSHA_MOBILE` уже настроен удалённый `origin`:
  - `git@github.com:MaxCorpOrg/GOSHA_MOBILE.git`
- Ветка `main` уже отправлена в GitHub и отслеживает:
  - `origin/main`
- Новый мобильный проект уже собирается локально как самостоятельный клиент:
  - `assembleClientDebug`
  - `testClientDebugUnitTest`
- Выполнен живой прогон нового клиента на телефоне `TECNO LI9`:
  - код `MJ6SG97A` принят
  - приложение сохранило `panel_url = http://<PRIMARY_PLATFORM_SERVER>:18876`
  - исторически приложение сохранило `hub_url` на voice WebSocket `/mcp/`; это больше не считается корректным default для `edge_hub_url`
  - приложение сохранило `robot_id = gosha-main`
  - приложение перевело телефон в сеть `GOSHA-A-1BE1`
  - локальный экран `HotspotPortalActivity` открыт
- Выполнен отдельный санитарный аудит пользовательского слоя на китайские символы и чужие музыкальные привязки.
- Что найдено:
  - активный музыкальный слот `music-tools` в подписках и панели
  - старый пользовательский сервис `music`
  - пример пользовательского сервиса `music.player`
  - китайские пользовательские строки не в панели, а в активной прошивке `gosha-v1`:
    - описания `MCP`-инструментов
    - runtime-логи
    - сообщения ошибок
    - описание `press-to-talk`
- Что уже сделано:
  - `music-tools` принудительно отключён и больше не предлагается оператору
  - чекбокс музыки убран из панели
  - вместо старого примера оставлен нейтральный зарезервированный идентификатор `gosha.media.stub`
  - карточка подключения робота по умолчанию предлагает `Платформа Гоша`, а не `совместимый облачный режим`
  - если у робота не записан `ROBOT_BACKEND_MODE`, платформа теперь тоже считает это режимом `Платформа Гоша`
  - `platform/add_robot.sh` создаёт новых роботов уже с `ROBOT_BACKEND_MODE=self_hosted_xiaozhi`
  - пользовательский HTML панели проверен: китайских символов нет
  - активные пользовательские строковые литералы в изменённых файлах `gosha-v1` переведены на русский
  - локальный smoke-check `assistant-control/catalog` дополнительно исправлен после найденного дефекта `list_robot_ids()`

## Где что искать

- Общая карта:
  - `<LOCAL_WORKSPACE>/docs/GOSHA_PROJECT_MAP_RU.md`
- Платформа и панель:
  - `<LOCAL_WORKSPACE>/platform`
- Переопределения синтеза и совместимого server-side слоя:
  - `<LOCAL_WORKSPACE>/backend/overrides`
- Быстрое переключение живого голосового профиля:
  - `<LOCAL_WORKSPACE>/ops/set_live_voice_profile.py`
  - после смены профиля нужно перерендерить серверный `backend`
- Локальный запуск панели для smoke-проверки:
  - `<LOCAL_WORKSPACE>/bin/run_local_gosha_gateway.sh`
  - `<LOCAL_WORKSPACE>/bin/run_local_gosha_panel.sh`
  - только после этого локально открывается `http://127.0.0.1:18876`
- Локальные секреты разработчика:
  - `<LOCAL_WORKSPACE>/GOSHA_API`
- Серверные секреты и рабочая конфигурация:
  - `<SERVER_PROVIDER_ENV>`
  - `<SERVER_BACKEND_CONFIG>`
- Прошивка:
  - `<FIRMWARE_WORKSPACE>`
- Мобильный клиент:
  - `<MOBILE_WORKSPACE>`

## Главный незакрытый блокер

- Базовая связность уже не блокер:
  - панель, шлюз, совместимый `backend`, новая прошивка и мобильный путь уже живы;
  - `SileroTTS` уже поднят как живой тестовый контур;
  - новая прошивка уже сама оставляет прямой след в платформе.
- Текущий главный незакрытый блокер теперь продуктовый:
  - схема уже правильная, но панель всё ещё тяжеловата по коду и деталям;
  - нужно проверить живьём, достаточно ли оператору нового первого экрана без дублирующего списка роботов;
  - нужно дальше упростить и разрезать перегруженный `panel_index.html` без слома backend.
- Уже сделан ещё один шаг в сторону простого пульта:
  - верхняя сводка по флоту, скрытие второстепенных действий и отдельная страница робота уже работают;
  - следующий шаг — проверить, что ещё можно убрать или спрятать без потери рабочих сценариев.
- Новый прямой сигнал уже проведён в API и UI:
  - панель видит `last_seen`, `board_name`, `board_ip`, `app_version`, `remote_addr`;
  - в рабочем окне робота уже есть блок `След устройства в платформе`;
  - следующий шаг — проверить, достаточно ли этого оператору и не нужно ли поднять статус ещё выше.
- Для `GOSHA_MOBILE` пользовательский контур уже рабочий, но всё ещё требует UX-доводки:
  - ложная эвристика "подключено через панель" уже закрыта;
  - нужно точнее показать разницу между сетью робота, домашней сетью и уже подтверждённой связью с платформой;
  - нужно по возможности убрать зависимость от лишнего перезапуска приложения.
- Для `GOSHA_FIRMWARE` текущая рабочая точка слова пробуждения — порог `38`.
  Дальше нужен уже отдельный акустический пакет: ложные срабатывания, дальнее распознавание, влияние динамика.
- Важно:
  - китайские символы в репозитории ещё встречаются в комментариях, неактивных платах и `zh-*` ресурсах;
  - музыкальный слот остаётся отключённым и сохранён только как `gosha.media.stub` для будущего собственного расширения.

## Ближайший приоритет

1. Следующим пакетом довести панель до действительно простого операторского центра по карточкам роботов.
2. На странице робота сделать максимально простой верхний статус:
   - робот на связи;
   - давно не выходил на связь;
   - робот в домашнем Wi‑Fi;
   - робот всё ещё только в режиме hotspot.
3. Для `GOSHA_MOBILE` пересмотреть UX подключения с учётом новой прошивки и уже честного блока `connectivity`.
4. Для `GOSHA_FIRMWARE` продолжить наблюдение и акустическую доводку слова пробуждения на пороге `38`.
5. Если потребуется смена ключа или профиля поставщика, перезапустить лёгкий контур:
   - `bash <SERVER_APP_ROOT>/ops/install_server.sh --phase panel`
6. Потом продолжить вторую очередь панели:
   - выбор движка синтеза речи;
   - экранные профили;
   - пробуждение;
   - рабочая база знаний.
