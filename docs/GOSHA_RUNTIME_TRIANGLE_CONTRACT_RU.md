# Контракт реального времени «робот — приложение — панель»

Дата фиксации: `2026-07-22`.

## Назначение

Этот документ задаёт общий расширяемый контракт наблюдаемости для трёх равноправных сторон продукта `Гоша`:

- робота и его прошивки;
- одного или нескольких мобильных приложений;
- одной или нескольких операторских панелей.

Контракт не заменяет управляющие протоколы, OTA или голосовой WebSocket. Он связывает их общими событиями, идентификаторами сессий и задач, чтобы панель могла объяснить не только итоговое состояние, но и путь к нему.

## Основные правила

1. Единственная обязательная область агрегации — `robot_id`. Один робот может одновременно иметь несколько мобильных клиентов и панелей.
2. `robot_id` и вид источника берутся из проверенного сервером контекста. Идентификатор прошивки и панели также выводится из их сессии; мобильный `source.id` является заявленным идентификатором установки внутри уже разрешённой области конкретного робота.
3. Повторная отправка одного `event_id` безопасна: событие не увеличивает статистику второй раз.
4. События не содержат пароли, токены, SSID, OTA-адреса, WebSocket-адреса и другие внутренние адреса инфраструктуры.
5. Сбой отправки события не должен блокировать сеть, звук, управление роботом или восстановление Wi-Fi. Клиенты используют ограниченную очередь и повторную отправку.
6. Изменение несовместимого формата требует новой версии схемы. Дополнительные поля внутри текущей схемы допустимы.
7. Наличие старого события не означает живую связь: панель отдельно оценивает свежесть сигнала. Для компонентов и связей базовое окно составляет 120 секунд, для незавершённых задач — 15 минут.
8. Журнал ограничен по каждому роботу. Базовый предел — 10 000 событий, он настраивается через `RUNTIME_EVENTS_MAX_PER_ROBOT`; агрегированные счётчики при очистке старых строк сохраняются.
9. Платформа удаляет запрещённые ключи и скрывает URL, заголовки авторизации, токены и значения вида `SSID=...`, даже если они вложены в обычное текстовое поле. Это последний защитный рубеж; источники всё равно обязаны не включать такие данные в событие.
10. Внутри одного `source.instance_id` поле `sequence` монотонно растёт. Запоздавший повтор сохраняется в журнале и статистике, но не откатывает уже более новое состояние компонента или связи.

## Конверт события версии 1

```json
{
  "schema_version": "gosha.runtime.event.v1",
  "event_id": "5b2373ea-3f31-4d97-b3c3-1ebd49d76567",
  "event_type": "mobile.network.state_changed",
  "source": {
    "kind": "mobile",
    "id": "panel-installation-01",
    "instance_id": "process-session-01",
    "app_version": "1.0.0"
  },
  "subject": {
    "robot_id": "robot-01",
    "client_id": "optional-client-id",
    "panel_id": "optional-panel-id"
  },
  "trace": {
    "session_id": "app-session-01",
    "correlation_id": "wifi-recovery-01",
    "causation_id": "previous-event-id"
  },
  "occurred_at": "2026-07-22T12:00:00Z",
  "sequence": 42,
  "severity": "warning",
  "state": {
    "domain": "connectivity",
    "name": "home_wifi",
    "status": "lost"
  },
  "link": {
    "kind": "mobile_robot",
    "status": "unavailable"
  },
  "task": {
    "id": "wifi-recovery-01",
    "kind": "wifi_recovery",
    "status": "running"
  },
  "error": {
    "code": "wifi_lost",
    "message": "Домашняя сеть временно недоступна",
    "retryable": true
  },
  "metrics": {
    "retry_count": 1,
    "latency_ms": 120
  },
  "attributes": {
    "trigger": "network_callback"
  }
}
```

Платформа всегда заменяет `source.kind` и `subject.robot_id` значениями из проверенного контекста. Для прошивки и панели она также сама выводит `source.id` из ключа устройства или операторской сессии. Для мобильного клиента проверяется доступ к роботу и формат постоянного идентификатора установки. Время приёма добавляется как `received_at`.

## Виды источников

| `source.kind` | Что означает | Как проверяется |
|---|---|---|
| `robot` | конкретное физическое устройство | привязка `Device-Id` к роботу и ключ устройства |
| `mobile` | установка мобильного клиента | мобильный ключ доступа к выбранному роботу и проверенный формат постоянного идентификатора установки |
| `panel` | экземпляр операторской панели | операторская сессия |
| `service` | внутренняя служба платформы | внутренний вызов без публичного доверия к телу запроса |

`source.id` обозначает устойчивый экземпляр, а `source.instance_id` — текущий процесс или сеанс. Это позволяет отличать несколько телефонов и панелей, не меняя основу.

## Связи

Минимальный словарь `link.kind`:

- `robot_platform` — устройство видит платформу;
- `mobile_platform` — приложение видит платформу;
- `panel_platform` — панель видит платформу;
- `mobile_robot` — приложение видит локальный или управляемый канал робота;
- `panel_robot` — панель имеет подтверждённый канал управления роботом.

Статус связи передаётся в `link.status`: `available`, `unavailable`, `connecting`, `degraded`, `unknown`. Новые виды связей можно добавлять без изменения конверта.

## Задачи, ошибки и статистика

- Одна логическая операция получает устойчивый `task.id` и `trace.correlation_id` во всех трёх контурах.
- Активные статусы задачи: `queued`, `running`, `waiting`, `retrying`.
- Завершающие статусы: `completed`, `failed`, `cancelled`, `timed_out`.
- Ошибка имеет машинный `error.code`, безопасное описание и признак возможности повтора.
- `metrics` содержит числовые измерения конкретного события. Платформа отдельно считает количество событий по типу, источнику и важности.

## Маршруты платформы

- `POST /api/mobile/robots/{robot_id}/events` — события приложения; используется существующая мобильная авторизация.
- `POST /gosha/events` — события привязанной прошивки; используется `Device-Id` и ключ устройства из ответа OTA.
- `POST /api/operator/robots/{robot_id}/events` — события операторской панели; используется операторская сессия.
- `GET /api/operator/robots/{robot_id}/runtime-events` — агрегированное состояние и ограниченный журнал.
- `GET /api/operator/runtime-events/stream` — поток событий для автоматического обновления панели.

Существующий мобильный маршрут состояния робота дополнен полем `triangle`. Старые клиенты продолжают работать без изменений.

Ошибка проверки схемы возвращается как `422 validation_error` и считается окончательной для конкретного тела события. Временная ошибка хранения возвращается как `503 transient_error`. Клиенты удаляют событие без повтора только после `413` или `422`; ответы авторизации, несовпадение версии развёртывания, сетевые ошибки и ответы `5xx` остаются в ограниченной очереди для повторной доставки.

## Первый сквозной сценарий

Первым контрольным сценарием является восстановление Android-клиента после временной потери домашнего Wi-Fi без перезапуска приложения:

1. приложение отправляет `mobile.network.state_changed` со статусом потери сети;
2. задача `wifi_recovery` переходит в `running` или `retrying`;
3. обнаружение и локальная проверка робота передают свои результаты с тем же `correlation_id`;
4. после возврата сети приложение повторно находит робота и завершает задачу;
5. панель в реальном времени показывает обе стороны связи, ход задачи, ошибки повторов и итоговое восстановление.

Этот сценарий проверяет архитектуру, но не ограничивает её: те же интерфейсы используются для OTA, команд, голосовой сессии, аппаратных ошибок и статистики.

## Разделение репозиториев

- `GOSHA_PLATFORM` владеет схемой, проверкой доступа, хранением, агрегацией и операторским отображением.
- `GOSHA_MOBILE` владеет устойчивым идентификатором установки, сетевыми событиями, локальным обнаружением и очередью доставки.
- `GOSHA_FIRMWARE` владеет неблокирующими событиями загрузки, сети, автомата состояний и аппаратных ошибок.

Каждый репозиторий развивается в отдельной ветке. Совместимость проверяется по этому контракту; исходники между репозиториями не копируются.
